{
  "name": "wealth-builder",
  "private": true,
  "version": "1.0.0",
  "description": "Wealth Builder — static SPA with Vercel serverless APIs",
  "scripts": {
    "build": "echo \"Static SPA — no build step\"",
    "vercel-build": "echo \"Static SPA — no build step\"",
    "start": "echo \"Use 'vercel dev' for local; this project is static.\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {}
}
